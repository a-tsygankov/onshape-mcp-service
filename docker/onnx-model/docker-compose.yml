version: "3.9"

services:
  onnx-model:
    image: mcr.microsoft.com/azureml/onnxruntime:latest
    container_name: onnx-model
    command: ["sleep", "infinity"]
    volumes:
      - ./models:/models
